These settings apply to GitHub Copilot only.

When making a pull request, create the branch for the PR following this naming convention: `copilot/<type>/<slug>`
- Example: `copilot/feature/add-encryption`, `copilot/fix/auth-bypass`

This will distinguish user vs. Copilot-created branches
